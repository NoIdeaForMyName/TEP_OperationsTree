#include "Variable.h"
#include <string>

using namespace  std;

operators = { "+", "-", "*", "/", "sin", "cos" };

Variable::Variable(string str)
	: str(str)
{
	for (int i = 0; i < operators->size(); i++)

	for (int i = 0; i < str.size(); i++)
	{
		char val = str[i];

	}
}

